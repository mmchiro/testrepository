import numpy as np
import pandas as pd
new_array = np.arange(0,10).reshape(2,5)

#The output is a matrix
#print(new_array)

#Change it to a dataframe
#You will see that the row and column of dataframe being numbered
#df = pd.DataFrame(data=new_array)
#print(df)


#Name the columns as 'A','B','C','D','E'
#df=pd.DataFrame(data=new_array, columns =['A','B','C','D','E'])
#print(df)
